# 把 Claude Code / Codex 的完成或中断通知发到飞书企业号

原来的 `notify.py` 只能单向推送（webhook 往群里发一条消息就完了）。
现在加了 `notify_to_enterprise.py`，通知发到**企业自建应用**，支持你在手机上回复并
自动转给对应的 Claude 窗口继续干活 —— 结合 `bridge.py`，形成完整的双向闭环。

---

## 工作原理

1. **每个 Claude 会话对应一个飞书私聊窗口**

   钩子第一次发通知时，用企业应用的 API 创建一个私聊（你和机器人两人对话的那种），
   标题格式 `[Claude] 项目名 · 会话ID前8位`，以后该会话的通知都往这个窗口发。

   **你在哪个窗口回复，机器人就知道是回给哪个会话的**，不会串 —— 即使电脑上同时开着
   10 个 Claude Code 窗口。

2. **回复自动转发给 Claude**

   你在私聊窗口回复的消息，桥程序（`bridge.py`）收到后，根据 `chat_id` 反查出对应的
   `session_id`，用 `claude -p -r <session_id>` 续接那个会话，结果再发回同一个私聊窗口。

3. **与原 webhook 方案并存**

   `notify_config.json` 的 `webhook` 字段保留不动（可能别的地方还在用），新方案只看
   新增的三个字段；三个字段都填了才启用企业通知，否则静默跳过，不会报错。

---

## 前置条件

1. **飞书企业自建应用已创建并发布**（就是 `bridge.py` 用的那个应用）
2. **开通了「接收消息」和「发送消息」权限**（已在前面的步骤里开过了）
3. **拿到你的 open_id**

   在飞书开发者后台 → 你的应用 → **权限管理**页面的右上角，点自己头像会弹出一个卡片，
   里面有 `open_id`（形如 `ou_xxxxxxxx`）。或者用 API 查：

   ```bash
   curl -X POST 'https://open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal' \
     -H 'Content-Type: application/json' \
     -d '{"app_id":"你的app_id","app_secret":"你的app_secret"}' | jq -r '.tenant_access_token'

   # 拿到 token 后，查当前用户（你自己）
   curl 'https://open.feishu.cn/open-apis/contact/v3/users/me' \
     -H 'Authorization: Bearer <上面拿到的token>' | jq -r '.data.user.open_id'
   ```

---

## 配置步骤

### 1. 给 `notify_config.json` 加三个字段

`%USERPROFILE%\.claude\hooks\notify_config.json` 里加：

```json
{
  "enabled": true,
  "webhook": "https://open.feishu.cn/open-apis/bot/v2/hook/...",
  "min_seconds": 60,
  "summary_chars": 300,
  "enterprise_app_id": "cli_xxxxxxxxxxxxxxxxxx",
  "enterprise_app_secret": "请在飞书开发者后台查看，不要提交到 git",
  "user_open_id": "ou_xxxxxxxx"
}
```

把 `user_open_id` 换成你实际的。前两个跟 `bridge_config.json` 里的一样。

### 2. 改 `settings.json` 的钩子 command

`%USERPROFILE%\.claude\settings.json` 里，把原来的 Stop / StopFailure / Notification
钩子的 `command` 字段从：

```
python %USERPROFILE%\\.claude\\hooks\\notify.py stop
```

改成：

```
python %USERPROFILE%\\.claude\\feishu-bridge\\notify_to_enterprise.py stop
```

三个钩子都要改（`stop` / `stopfail` / `notification`），最后一个词（事件名）别动。

完整示例：

```json
{
  "hooks": {
    "Stop": [{
      "matcher": "*",
      "hooks": [{
        "type": "command",
        "command": "python %USERPROFILE%\\.claude\\feishu-bridge\\notify_to_enterprise.py stop",
        "timeout": 15000
      }]
    }],
    "StopFailure": [{
      "matcher": "*",
      "hooks": [{
        "type": "command",
        "command": "python %USERPROFILE%\\.claude\\feishu-bridge\\notify_to_enterprise.py stopfail",
        "timeout": 15000
      }]
    }],
    "Notification": [{
      "matcher": "*",
      "hooks": [{
        "type": "command",
        "command": "python %USERPROFILE%\\.claude\\feishu-bridge\\notify_to_enterprise.py notification",
        "timeout": 15000
      }]
    }]
  }
}
```

### 3. 重启 Claude Code

钩子配置改了要重启才生效（或者用 `/hooks` 但这个会话里用不了）。

### 4. 测试

```bash
cd %USERPROFILE%\.claude\feishu-bridge
python notify_to_enterprise.py test
```

看到 `发送：成功` 并且飞书里收到一条私聊消息，就说明通了。

---

## 使用

1. **电脑上跑一个耗时任务**

   ```bash
   claude "帮我把 xxx 目录下所有 Python 文件的 docstring 补全"
   ```

2. **干完或出错，手机上飞书收到私聊通知**

   标题 `[Claude] 项目名 · abc12345`，内容是任务完成/出错/等你操作等。

3. **在那个私聊窗口回复**

   比如：

   ```
   再把测试也补上
   ```

   或者出错时：

   ```
   换个思路，用 XXX 库试试
   ```

4. **桥自动续接那个会话**

   你回复几秒后，同一个私聊窗口里会收到 Claude 的回复（可能很长，分几条发）。
   继续对话就行，跟在电脑前用 `claude` 命令一样。

---

## 排查

### 通知没发出来

1. 看 `notify_config.json` 里三个字段填了没
2. `python notify_to_enterprise.py test` 看报什么错
3. 检查 `app_id` / `app_secret` 是不是跟 `bridge_config.json` 里的一致
4. 确认应用已发布（`code 232034` = 没发布）

### 回复了没反应

1. 确认 `bridge.py` 在跑（`tasklist | grep python` 看有没有）
2. 看 `bridge.log`，搜 `收到` 能不能找到你的回复
3. 检查 `bridge_state.json` 的 `chats` 字段，看 `chat_id` 和 `session_id` 对应关系存了没

### 通知发到了错误的窗口

每个 `session_id` 只对应一个 `chat_id`，首次通知时建立映射并写进 `bridge_state.json`。
如果发现对应关系错了，手动编辑那个文件把 `chats` 里对应的条目删掉，下次通知会重建。

---

## 与 webhook 方案对比

| 方案 | 单向/双向 | 要不要公网 IP | 能不能多窗口对应 | 回复后要不要切回电脑 |
|------|-----------|---------------|------------------|---------------------|
| webhook（原 `notify.py`） | 单向 | 不要 | 不能 | 要 |
| 企业应用（新方案） | 双向 | 不要（长连接） | 能 | 不要 |

新方案是 webhook 的完全上位替代，唯一多出来的成本是得创建一个飞书企业（免费，不要营业执照）。
