# AI 会话监控与远程操控 —— 方案

一句话讲清：**电脑上跑着的 Claude / Codex 会话，在手机上能看见、能接着指挥。**
两条通道并存：飞书负责「叫你」，网页负责「让你干活」。

---

## 一、先说清楚做不到的事

这三条是硬限制，不是没做，是做不了。省得你后面以为是 bug。

### 1. 操控不了你屏幕上那个窗口

Claude Code 的窗口不对外开接口，任何程序都插不进去。

所以「远程操控」的真实含义是：**新起一个进程续接那个会话的上下文**，
命令是 `claude -p -r <session_id>`。上下文是通的（它记得之前聊的），
但结果不会出现在你屏幕上那个窗口里。

### 2. 判断不了「这个窗口现在还开着吗」

试过四种办法，全部失败，有记录：

| 办法 | 结果 |
|---|---|
| 读进程命令行参数 | 62 个 node/claude 进程，命令行里没有 session_id |
| 查 `Temp/claude/<session_id>/` 目录时间 | 反映的是「上次跑后台任务」，不是窗口状态 |
| `handle64 -u ".claude\projects"` | 空 |
| `handle64 -u "<具体 session_id>"` | 空 |

`handle64` 本身是好的（搜 "log" 搜出一堆别的进程的句柄），搜会话文件是真的没有。
根因：Claude 写 JSONL 是「追加一行、立刻关闭」，不长期持有句柄，
所以操作系统层面看不出哪个进程属于哪个会话。

**替代方案**：按文件最后修改时间分三档，见下面「活跃度判定」。

### 3. Claude Desktop 的云端会话看不到

Claude Desktop 有一部分会话存在云端（claude.ai 账号里），本地没有文件。
只有走 Claude Code 通道的会话才在 `~/.claude/projects/` 下留 JSONL。

实测确认：`%APPDATA%\Claude\` 和 `%LOCALAPPDATA%\AnthropicClaude\` 下
只有配置文件和程序，没有会话数据库。

---

## 二、活跃度判定

| 档位 | 判据 | 含义 |
|---|---|---|
| 🟢 活跃 | 会话文件 5 分钟内有写入 | 几乎肯定开着，而且刚在动 |
| 🟡 可能开着 | 2 小时内有写入 | 大概率开着，你只是没说话 |
| ⚪ 已停 | 更早 | 可能关了，也可能开着挂机 |

⚪ 这档也能发消息 —— 因为发消息本来就是新起进程续接，跟窗口开没开无关。
分档只影响列表好不好看、你先看哪个。

---

## 三、架构

```
        你的手机                        你的电脑
    ┌─────────────┐
    │  飞书 App   │◄─────── 通知（钩子推送）────────┐
    │             │                                 │
    │  「跑完了」  │──── 回复 ────┐                  │
    └─────────────┘               │                 │
                                  ▼                 │
    ┌─────────────┐        ┌──────────────┐         │
    │ Safari 浏览器│◄──────►│  bridge.py   │         │
    │             │        │  飞书长连接   │         │
    │ 列表/详情页  │        └──────┬───────┘         │
    └──────┬──────┘               │                 │
           │                      │                 │
           │  HTTP/局域网          │                 │
           ▼                      ▼                 │
    ┌──────────────────────────────────────┐        │
    │        web_server.py (Flask)         │        │
    │  密码登录 · 列表 · 详情 · 发消息      │        │
    └──────┬───────────────────────┬───────┘        │
           │                       │                │
           ▼                       ▼                │
    ┌──────────────┐      ┌────────────────┐        │
    │session_scanner│      │ session_lock   │        │
    │  扫会话文件   │      │  防同时操作     │        │
    └──────┬───────┘      └────────┬───────┘        │
           │                       │                │
           ▼                       ▼                │
    ┌──────────────────────────────────────┐        │
    │  ~/.claude/projects/*/*.jsonl        │        │
    │  ~/.codex/sessions/*/*/*/*.jsonl     │        │
    │  bridge_state.json（线名映射）        │        │
    └──────────────────────────────────────┘        │
                                                     │
    ┌──────────────────────────────────────┐        │
    │  notify_to_enterprise.py（钩子）─────┼────────┘
    │  任务完成/中断时推送到飞书             │
    └──────────────────────────────────────┘
```

**飞书和网页共用底层**：同一套扫描器、同一把锁、同一个状态文件。
你在飞书 `/新 改网站`，网页列表里那条就显示成「改网站」；
在网页上给它发消息，飞书那条线的 `session_id` 也跟着更新。

---

## 四、防撞车

两条通道操作同一个会话会出事：两个 `claude -p` 进程同时续接同一个 `session_id`，
谁先写完谁覆盖谁，上下文会乱。

**做法：一个 session_id 一把锁。**

- 飞书要发消息 → 先拿锁 → 拿不到就回「这个会话正在跑，等它回来」
- 网页要发消息 → 同一把锁 → 拿不到就在页面上显示「正在跑」并禁用输入框
- 干完（不管成没成）都释放锁

锁存在文件里（`session_locks.json`），带过期时间（默认 30 分钟，跟任务超时对齐），
防止程序崩了锁不释放导致会话永久卡死。

---

## 五、Web 界面

### 列表页（首屏）

```
┌─────────────────────────┐
│  AI 任务监控        🔄  │
├─────────────────────────┤
│ 🟢 改网站                │  ← 飞书起的线，显示线名
│    3 分钟前 · 运行中     │
│    正在分析 854 个文件…  │
├─────────────────────────┤
│ 🟡 claude-files          │  ← 没线名就显示项目名
│    22 分钟前 · 完成      │
│    改完了，导航是三栏…   │
├─────────────────────────┤
│ ⚪ 数据分析  [Codex]     │
│    6 小时前 · 完成       │
│    8 月销量是…           │
└─────────────────────────┘
```

- 默认只显示 🟢 和 🟡，⚪ 折叠在「显示更早的」后面（511 个历史会话不能全铺出来）
- 刚完成 / 刚中断的加高亮，你一眼看到该管哪个
- 正在被占用的（锁着）标 ⏳

### 详情页

```
┌─────────────────────────┐
│ ← 返回      改网站      │
│             🟢 运行中    │
├─────────────────────────┤
│  你 · 22:15             │
│  把目录里的文件分类      │
│                         │
│  Claude · 22:16         │
│  总计 854 个文件…        │
│                         │
│  你 · 22:40             │
│  再按大小排一下          │
│                         │
│  ⏳ 正在跑…（1 分 12 秒）│
├─────────────────────────┤
│ ┌─────────────────┬───┐ │
│ │ 说点什么…       │发 │ │
│ └─────────────────┴───┘ │
│ [只读] [聊天]  ⚙       │
└─────────────────────────┘
```

- 对话历史从 JSONL 里读，只读最后 30 轮（够你看清卡在哪，又不会太慢）
- 底部档位按钮跟飞书的 `/只读`、`/聊天` 一样
- 会话被锁着时输入框禁用，显示「正在跑」

### 智能轮询

- 有 🟢 会话 → 每 3 秒刷一次
- 全是 🟡/⚪ → 每 30 秒刷一次
- 页面切到后台 → 停止轮询（省电，`visibilitychange` 事件）

---

## 六、接口

| 方法 | 路径 | 干什么 |
|---|---|---|
| GET | `/` | 列表页 |
| POST | `/login` | 密码登录，发 Cookie |
| GET | `/api/sessions` | 会话列表（JSON），带 `?level=active\|recent\|all` |
| GET | `/api/sessions/<sid>` | 某个会话的对话历史 |
| POST | `/api/send` | 给会话发消息，参数 `sid` / `msg` / `perm` |
| GET | `/api/task/<task_id>` | 查异步任务状态（发消息是异步的） |
| GET | `/session/<sid>` | 详情页 |

发消息是异步的：POST 立刻返回 `task_id`，前端拿这个 id 轮询状态。
不然手机上等几分钟会超时。

---

## 七、安全

- 密码存哈希（`werkzeug.security`），不存明文
- Cookie 设 `HttpOnly`，30 天过期
- **默认只监听 `0.0.0.0:8000` 且要求密码** —— 局域网内别人知道 IP 也进不来
- 危险操作沿用飞书那套：9 条危险模式匹配上就要二次确认
- 配置文件里的 App Secret 等凭据不进 git（`.gitignore` + `*.example` 模板）

内网穿透（以后要用的话）额外注意：
- 穿透之后就是公网可达，密码必须够强
- 建议加 IP 白名单或者只在需要时开穿透

---

## 八、目录结构

```
feishu-bridge/                    → 以后改名 ai-session-manager
├── bridge.py                     飞书长连接（已有）
├── session_scanner.py            会话扫描（已有，要改造）
├── session_lock.py               会话锁（新）
├── web_server.py                 Flask 服务（新）
├── templates/                    页面（新）
│   ├── login.html
│   ├── list.html
│   └── detail.html
├── static/
│   └── style.css
├── notify_to_enterprise.py       钩子推送（已有）
├── bridge_config.json            配置（不进 git）
├── bridge_config.example.json    配置模板（进 git）
├── bridge_state.json             状态（不进 git）
├── test_*.py                     测试（已有 125 项 + 新增）
├── README.md                     开源说明
├── LICENSE                       MIT
└── .gitignore
```

---

## 九、三阶段

### 第一阶段（现在做）

1. 扫描器改造：三档活跃度 + 读线名 + 按档过滤
2. 会话锁
3. Flask 后端 + htmx 前端
4. 飞书桥接上新模块（`/列表` 显示所有窗口）
5. 测试

**你能验证的**：手机浏览器打开 `http://电脑IP:8000`，输密码，
看到所有会话列表，点进去能看对话、能发消息，飞书那边照样能用。

### 第二阶段

- 整理成可开源的结构，发 GitHub
- README 写清楚装法用法，配置模板脱敏
- 检查历史提交里没有凭据（这个项目还没建 git，从头就干净）

### 第三阶段

VSCode 插件还是 Claude Desktop 插件，等前两阶段跑顺了再定 —— 
先看实际用起来缺什么。可能根本不需要插件，浏览器加个「添加到主屏幕」就够了。

---

## 十、技术选型

| 选什么 | 为什么 |
|---|---|
| Flask | 比 FastAPI 简单，这个场景不需要异步性能。本机 pip 装一个包就行 |
| htmx | 不用写 JavaScript 构建流程，一个 `<script>` 标签引进来。改页面就是改 HTML |
| 原生 CSS | 手机优先的布局用不着框架，几十行搞定 |
| 文件存状态 | 已经有 `bridge_state.json` 了，不引数据库 |
| 轮询不用 WebSocket | 智能轮询够用，WebSocket 多一层连接管理和断线重连，不值得 |

都是「装一个包就能跑」的东西，不需要 node、不需要构建、不需要数据库。
