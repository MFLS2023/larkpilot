# 飞书遥控 Claude Code

在飞书群里发一句话，你电脑上的 Claude Code 就去干活，干完把结果发回群里。
人在外面用手机就行。

---

## 它是怎么连上的（顺手打消一个担心）

不是「在你家路由器上开个门等外面进来」，而是**你的电脑主动往飞书拨一条线**，
飞书顺着这条线把你的消息推回来。

跟微信客户端连微信服务器是一回事。**别人从外面根本摸不到你的机器**，
不用公网 IP、不用改路由器、不用装内网穿透工具。

---

## 第一步：建一个免费飞书团队（你现在是个人版，必须做这步）

飞书的规定：只有「企业自建应用」能用长连接，个人版账号没有开发者权限。
但建团队是免费的，**不需要营业执照**。

1. 打开飞书 → 点左上角你的头像 → **创建团队**（或「创建企业」）
2. 团队名随便填，比如「我自己」
3. 建完之后你就是这个团队的管理员，自动有开发者权限

建好后左上角会显示团队名字，不再是「个人版」。

---

## 第二步：建应用，拿两串钥匙

1. 浏览器打开 https://open.feishu.cn/app
2. 点 **创建企业自建应用**
3. 名字填「Claude 遥控」，描述随便写，图标可以不传
4. 创建完会跳进应用详情页

### 拿钥匙

左边菜单 → **凭证与基础信息**，页面上有两行：

- **App ID** —— 形如 `cli_a1b2c3d4e5f6`
- **App Secret** —— 点「显示」才看得见，一长串

这两个先复制到记事本，等下要填。

> App Secret 等于你家钥匙，别截图发人、别提交到 GitHub。

### 开机器人能力

左边菜单 → **添加应用能力** → 找到 **机器人** → 点添加。

### 加权限

左边菜单 → **权限管理**，搜索并勾上这几个：

| 权限标识（搜索用） | 干什么用 |
|---|---|
| `im:message` | 收消息（事件订阅的基础） |
| `im:message:send_as_bot` | 以机器人身份发消息 |
| `im:chat` | 读群信息、管理群 |
| `im:message.resource:readonly` | 接收你发到群里的图片 |

勾完点页面上的 **批量开通**。

### 订阅「收到消息」事件 —— 这步顺序很重要

左边菜单 → **事件与回调** → **事件配置**

1. 先点 **添加事件**，搜 `接收消息`，勾上 **接收消息 v2.0**（`im.message.receive_v1`）
2. **订阅方式**那里选 **使用长连接接收事件**

> ⚠️ 选长连接的时候，**程序必须正在运行**，否则飞书那边保存不了（它要现场验证连得上）。
> 所以：先做完第三步把程序跑起来，再回来选这个。

### 发布

左边菜单 → **版本管理与发布** → **创建版本** → 可用范围选「全部成员」→ 提交。
你自己是管理员，直接通过。

---

## 第三步：填钥匙

两串钥匙放在**同目录的 `.env` 文件**里（比明文写在配置文件里安全，
程序启动时会自动读取；`bridge_config.json` 里只放非敏感设置）。

1. 打开目录 `%USERPROFILE%\.claude\feishu-bridge\`
2. 新建一个文本文件，命名为 `.env`（Windows 会提示没输扩展名，确认即可）
3. 内容就两行：

```
FEISHU_APP_ID=cli_xxxxxxxxxxxx
FEISHU_APP_SECRET=你的secret
```

4. 再打开 `bridge_config.json`，把 `cwd` 改成你想让它干活的目录；
   `permission` 保持 `read` —— 出厂默认最保守，自己用想全开改成 `full`

存盘。然后自查一下有没有填对：

```bash
python %USERPROFILE%\.claude\feishu-bridge\check_app.py
```

全是 `[OK]` 就往下走。

---

## 第四步：跑起来

```bash
python %USERPROFILE%\.claude\feishu-bridge\bridge.py
```

看到 `connected to wss://…` 就是连上了。**这个窗口别关**，关了就断。

连上之后回到浏览器，把第二步里「使用长连接接收事件」保存掉。

### 拉机器人进群

飞书里打开你要用的群 → 群设置 → **群机器人** → 添加 → 找到「Claude 遥控」。

（也可以直接在飞书搜索框搜应用名，跟它单独私聊，不用建群。）

---

## 第五步：绑定你自己（重要，跳过这步机器人不会理你）

安全设计是「默认谁都不许用」：第一次使用要互相认一次门。

1. 电脑上跑：

   ```bash
   python %USERPROFILE%\.claude\feishu-bridge\auth.py --code
   ```

   会打出一个 6 位数字码，**10 分钟内有效**。

2. 在飞书群里把这个 6 位数字发给机器人

3. 它回「绑定好了」就成了 —— 之后这个账号和这个群一直可用

> 码只有坐在电脑前的你能看到，所以别人拿不到绑定资格。
> 别把码转发给别人 —— 拿到码就能控制这台机器。

---

## 第六步：试一下

群里 @机器人 发：

```
/帮助
```

它会回一段说明。再试个真活：

```
看一下 claude-files 目录里有哪些文件
```

它会先回「收到，在做了……」，然后过一会儿把结果发回来。

---

## 能回什么

**直接说人话就行**，会记得上下文，可以接着上一句聊：

```
把 notify.py 的超时改成 30 秒
```
```
刚才那个文件再帮我加个注释
```

**命令：**

| 回这个 | 效果 |
|---|---|
| `/帮助` | 看说明 |
| `/新会话` 或 `/新` | 把当前这条线清空，从头开始 |
| `/状态` | 看当前在哪条线、引擎、档位、工作目录 |

---

## 同时开好几个任务（多条「线」）

一条「线」＝一个独立会话。三条线互不干扰，各自记着自己聊到哪了，
不会你问 A 的事它拿 B 的上下文回答。

| 回这个 | 效果 |
|---|---|
| `/新 改网站` | 开一条叫「改网站」的线，之后的话都算这条线上的 |
| `/新 查数据 codex` | 开一条线，指定用 codex 干活 |
| `/切 改网站` | 切回那条线，上下文还在 |
| `/切 改` | 名字打一半也行，只要能唯一对上 |
| `/列表` | 看有哪些线、各自在忙什么、当前在哪条 |
| `/删 改网站` | 删掉那条线（正在跑活的删不掉） |

**手机上长什么样：**

```
你：/新 改网站
它：开好了：「改网站」［claude］
    之后的话都算这条线上的。

你：把首页导航改成三栏
它：［改网站］收到，在做了……（claude，档位 full）

你：/新 查数据
它：开好了：「查数据」［claude］

你：帮我看一下上个月的销量表
它：［查数据］收到，在做了……

它：［改网站］改完了，导航现在是三栏……      ← 上面那个活干完了
它：［查数据］表里 8 月份是……
```

每条回复前面都带着 `［线名］`，所以两个活的结果混在一个群里也分得清。

**几个说明：**

- 不指定线就用「当前」那条。第一次直接发任务，会自动给你建一条叫「默认」的
- 一条线正在跑活时还能给别的线发任务，两边并行跑，回来各标各的名字
- `/列表` 里带 ⏳ 的就是还在跑的
- 危险操作等你确认时，会记住是哪条线提的。你中间切走看别的，回来说「确认」，
  做的还是当初那条线上那件事

---

## 换引擎（claude / codex）

| 回这个 | 效果 |
|---|---|
| `/codex 帮我看看这个仓库` | 这条线以后交给 codex，同时把这句话发过去 |
| `/codex` | 只换引擎，不发任务 |
| `/claude` | 换回 claude |

换引擎会把这条线的上下文清空 —— claude 的会话号 codex 认不了，反过来也一样。
想两个引擎同时用，开两条线：

```
/新 让claude改 claude
/新 让codex查 codex
```

两个引擎的权限档位是分开管的：

| 档位 | claude 那边 | codex 那边 |
|---|---|---|
| full | 所有工具全开 | `danger-full-access`（不沙箱） |
| write | 只给 Read/Write/Edit/Grep/Glob/搜索 | `workspace-write`（只能写工作目录） |
| read / chat | 只给 Read/Grep/Glob/搜索 | `read-only` |

**临时降权（只影响这一条）：**

| 回这个 | 效果 |
|---|---|
| `/只读 看看代码里有什么问题` | 只能看，改不了任何文件 |
| `/改文件 帮我改一下这个` | 能改文件，不能跑命令 |
| `/聊天 什么是 MCP` | 不给任何工具，纯问答 |

---

## 危险操作会问你一句

按你的要求，默认是**全开权限** —— 跟你坐在电脑前用是一样的。

但有一类操作我加了二次确认：**删了回不来的、影响别人的**。

命中的时候它会先回：

```
⚠ 这条像是「递归删除文件」

原话：把 dist 目录 rm -rf 掉

这类操作做完不好回退，而且你在手机上看不见它到底动哪个文件。
要做就回「确认」，不做回「取消」。
```

你回「确认」它才做，回「取消」就算了。

会问的有这些：递归删除、格式化磁盘、`git push --force`、`git reset --hard`、
删库删表、关机重启改注册表、部署生产、发布到公共仓库。

**普通改代码不会问**，实测「把超时改成 30 秒」「git commit」这类都直接放过。

不想要这层确认，把配置里 `confirm_dangerous` 改成 `false`。

---

## 配置项

| 项 | 说明 |
|---|---|
| `cwd` | 干活的目录。相对路径都从这里算 |
| `permission` | `auto` 自动权限 / `full` 完全跳过检查 / `write` 能改文件 / `read` 只读 / `chat` 纯聊天 |
| `confirm_dangerous` | 危险操作要不要问一句 |
| `timeout_seconds` | 单个任务最多跑多久，默认 1800 秒（30 分钟） |
| `allowed_user_ids` | 只认这些人发的消息。留空 `[]` 就是谁都行 |
| `allowed_chat_ids` | 只认这些群。留空就是所有群 |
| `truncate_reply` | `false`（默认）长回复分几条发完 / `true` 超长就截断 |
| `reply_max_chars` | 截断长度，**只在 `truncate_reply` 是 `true` 时才管事** |

**改配置不用重启程序**，下一条消息就按新配置走。
（只有 `app_id` / `app_secret` 改了要重启。）

---

## 长回复不会被截断了

飞书单条文本消息能装 **150 KB**（约 5 万汉字，官方文档写的，实测发 3 万汉字一条
成功）。所以绝大多数回复一条就发完了。

真超过一条装不下的时候，会自动分成几条连着发，每条尾部标着进度：

```
……（正文）

〔第 1/3 段〕
```

最后一条标 `〔完，共 3 段〕`，所以你在手机上一眼能看出还有没有下文。切的时候优先在
空行处断开，其次在换行处，都不行才硬切 —— 尽量不把一句话切两半。

**内容一个字都不会丢。** 这一点是测出来的，不是估的：12 万字的回复分 4 条发出去，
拼回来字数跟原文一致；6 万字不带任何换行的大块文本硬切成 2 条，也是一个字不差。

嫌消息条数多想回到老的截断行为，把 `truncate_reply` 改成 `true`。

---

## 开机自动跑

不想每次手动开窗口，双击这个：

```
%USERPROFILE%\.claude\feishu-bridge\装到开机启动.bat
```

它会在你的「启动」文件夹放一个快捷方式，以后开机自动在后台跑，没有黑窗口。

不想要了就双击 `取消开机启动.bat`。

---

## 出问题怎么查

**日志在这**：`%USERPROFILE%\.claude\feishu-bridge\bridge.log`

| 症状 | 原因 |
|---|---|
| 保存「长连接」时报错说连不上 | 程序没在跑。先跑起来再回去保存 |
| 机器人在群里，但发消息没反应 | 事件订阅里没勾「接收消息 v2.0」，或者订阅方式还是 HTTP 回调没切到长连接 |
| 回复「Not logged in」 | `settings.json` 里的 API key 没读到，跑 `--check` 看 |
| 一直「在做了」不出结果 | 任务太大超时了，看日志。把 `timeout_seconds` 加大或者拆小任务 |
| 报 `code 99991663` | 权限没开通，回权限管理页面确认那三个权限都批量开通了 |
| 同一句话干了两遍 | 正常情况不会（程序按 message_id 去重）。真出现了看日志有没有 `重复推送` |

---

## 会动到哪些文件

只有这个目录里的，别处不碰：

```
%USERPROFILE%\.claude\feishu-bridge\
├── bridge.py            主程序
├── bridge_config.json   你的配置（含 App Secret，别外传）
├── bridge_state.json    有哪些线、各自的会话号，程序自己维护
├── bridge.log           日志
├── check_app.py         查飞书配置对不对
├── test_bridge.py       逻辑自测（97 项）
├── test_parallel.py     并发压力测试（18 项）
├── test_e2e.py          真建群、真发消息（8 项，其中 2 项缺权限跳过）
└── test_loop.py         闭环测试（31 项，会往群里真发消息）
```

---

## 自己想验证的时候，跑这四个

按从快到慢排，出问题从上往下跑：

```bash
python check_app.py
```
查飞书那边配置对不对。只打 HTTP，不碰长连接，几秒钟。

```bash
python test_bridge.py
```
逻辑自测,97 项。危险词识别、命令解析、多线切换、codex 输出解析,最后会真调一次
claude 和 codex 各跑个算术题。大约五分钟。

```bash
python test_parallel.py
```
并发压力测试,18 项。20 个线程同时改状态看会不会互相覆盖,再让 claude 和 codex
真的同时跑,验证两条线的结果和会话号不串。大约一分钟。

```bash
python test_loop.py
```
闭环测试,31 项。**桥必须正在跑**。它伪造飞书事件喂给真实的处理函数,回复真的发到
「桥接自测群」—— 跑完打开飞书能看到那些消息。大约四分钟。

这个测的是别处测不到的东西:3 秒契约(处理函数必须立刻返回)、机器人不回自己的消息、
飞书重推同一条不能干两遍、两条线并行时结果不串。

---

## 几个说明

**先绑定再使用**：安全机制是默认拒绝 —— 没绑定的账号发任何消息，机器人
完全不理（这是故意的，不是坏了）。绑定方法见第五步。

**它和电脑上开着的窗口是两回事。** 飞书这边每条消息起一个独立的 `claude -p`
（或 `codex exec`）进程，跟你屏幕上那些窗口不共享上下文。

这一点没法绕过：Claude Code 的窗口没有对外接口，别的程序插不进去。所以飞书这边
用「线」来管多任务 —— 飞书里的线归飞书，屏幕上的窗口归屏幕，两边各自记自己的。
在外面用飞书开的线，回到电脑前想接着干，得让它把进展说一遍。

**群里所有人的消息都会被处理。** 你说了不用限定，所以配置里两个白名单都留空。
以后想限定，把自己的 user_id 填进 `allowed_user_ids`（日志里能看到 id）。

**长连接同一个应用最多 50 条，一台机器跑一份就够。** 同一个应用如果开了两份程序，
飞书只会随机推给其中一份，会漏消息。

**Codex 走的是 `codex exec` 命令行**（实测版本 codex-cli 0.146.0）。它的会话号和
Claude 的不通用，所以一条线只能属于一个引擎。Codex 没装也不影响，启动自查会提醒一句，
`/codex` 用不了而已，Claude 照常。
